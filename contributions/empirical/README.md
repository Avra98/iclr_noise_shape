These panels use immutable corrected experiment outputs and perform no training.

Main figures:

- `../../figures/empirical_stability_heatmaps.pdf`: CNN SiLU and MLP SiLU, two diagnostics across all saved step-size/batch cells.
- `../../figures/empirical_iid_mlp_dynamics.pdf`: fixed-axis occupancy, full transported growth, and adjacent-state harmonic gain in the iid MLP continuation.
- PNG counterparts support visual review.

Appendix control:

- `../../figures/empirical_cnn_confirmation.pdf`: unsuccessful confirmation of the discovery-selected CNN window, including every fixed bandwidth in the original screen.

`figures_main.tex` supplies main figure environments/captions, `experiment.tex` supplies main prose, and `appendix_empirical.tex` supplies detailed methods and the negative control. The root manuscript may shorten these contributions to fit its nine-page main-text budget.

`build_panels.py` reproduces all panels using the source paths recorded in `numerical_audit.json`. The local window-analysis dependency is copied verbatim from the completed experiment. All 72 main heatmap values are recomputed from raw gain pools before plotting. Original reports, traces, and checkpoints remain unchanged.

The additional `appendix_atlas.tex` compiles to seven pages under the official ICLR style and covers ten remaining vision architectures plus the four GPT metric views. `build_atlas.py` regenerates `../../figures/empirical_atlas_*.pdf` and PNG counterparts. `atlas_provenance.json` links accepted Part 1 vector sources and verifies 288 available scalar statistics, retaining all 84 missing gain cells and 18 archived cutoff escapes.

The two-page `appendix_nonconvex_empirical.tex` separates analytically recurrent smooth/constructed controls from finite moving-growth measurements and ordinary-network counterevidence. Its two PDF/PNG panels are regenerated by `build_nonconvex_panels.py`; `nonconvex_provenance.json` records raw tangent checks, matched variance, constructed-network growth, the completed 495-cell/135-escape census, and the paired ordinary CNN drift calculation. No prospective experiments enter these counts.

Main empirical panels are native 5.5-inch width, matching official ICLR text width; axis labels are 8 points and ticks 7 points before inclusion.

The main claims are deliberately narrow: log-expanding scalar compression occurs on both sides of the harmonic reference; an iid neural continuation has finite-window alternating two-lobe occupancy and positive transported finite-time growth while loss decreases. No plot establishes a stationary neural law, asymptotic top Lyapunov exponent, or universal harmonic-threshold cause of bimodality. The selected MLP checkpoint and the grid's run-window averages are distinct measurements.
