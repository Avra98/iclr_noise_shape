The declared follow-up crosses four architecture families (SiLU MLP, SiLU CNN, ResNet-32, and WideResNet-10-2) with batch sizes32,128,512,4096. Each continuation uses4096 IID-subset SGD updates at its parent's learning rate. Final checkpoint20000 is used when available; two large-batch cases use their last saved checkpoint10001, as labeled. This is a comparison of different trained states, not an intervention changing only batch size at one fixed state.

At the first completed-case snapshot,11 of16 continuations had finished. The later MLP checkpoint at batch4096 passes the fixed-axis two-lobe screen in the displayed windows512--1023 and1024--1535, but not2048--2559 or3072--3583. Its sliding-window scan contains additional passing intervals. The CNN at batches128 and512 has some passing64-update windows, but no passing windows of256 updates or longer. The other completed settings have no passing long window. Overlapping windows are dependent, and short-window candidates are exploratory. The CNN batch512 frozen Ritz direction has relative eigenpair residual0.0044, exceeding the chosen0.001 tolerance; its coordinate remains well-defined, but the axis is only an approximate leading direction.

The figure layout keeps the plotting frame fixed and separately measures its alignment with evolving leading Hessian Ritz vectors. The lower panels report harmonic, geometric, mean-absolute, and RMS scalar gains over training for both the fixed direction and the batch-gradient direction. Conditional probes are taken at even-update states; they are not averages across the two oscillation phases. Decreasing loss, positive scalar gains, and finite-window occupancy do not establish an invariant neural law or a universal harmonic-to-bimodal transition. Exact source paths, arrays, bandwidth checks, and run completion states are retained in the experiment package.
