\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/theory_inverse_return.pdf}
\caption{\textbf{An exact central-shape law for a returned multiplier.} Let $M=e^{V-U}$ with independent $U\sim\operatorname{Exp}(b)$, $V\sim\operatorname{Exp}(3)$, and $b=3+\alpha$. The saturated process $R^+=\min\{1,MR\}$ has positive local log drift in all three cases. (a) Its inverse root $m(-\alpha)=1$ crosses order one as $H$ crosses one. (b) The continuous part of the symmetrically signed invariant law is $(3/b)(\alpha/2)|x|^{\alpha-1}$ on $0<|x|<1$: a cusp, a flat center, or central depletion. Each endpoint also has an atom of mass $(1-3/b)/2$, which is not drawn as a density. These are analytic curves; Proposition~\ref{sc:solvable} in Appendix~\ref{sc:proofs} verifies the law exactly.}
\label{fig:inverse-return}
\end{figure}
